export type adminSidebar = {
  common: {
    logout: string;
    cancel: string;
  };
  sidebar: {
    dashboard: string;
    settings: string;
    logout: string;
  };
  logout: {
    confirmTitle: string;
    confirmDescription: string;
    action: string;
  };
};
